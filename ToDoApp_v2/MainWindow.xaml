<Window x:Class="SlimeTodo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:SlimeTodo.Views"
        xmlns:vm="clr-namespace:SlimeTodo.ViewModels"
        xmlns:converters="clr-namespace:SlimeTodo.Converters"
        Title="GajiGaji"
        Width="800"
        Height="600"
        MinWidth="600"
        MinHeight="400"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundBrush}"
        PreviewKeyDown="Window_PreviewKeyDown">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:IntToVisibilityConverter x:Key="IntToVisibilityConverter"/>
        <converters:StringToColorBrushConverter x:Key="StringToColorBrush"/>
    </Window.Resources>

    <Grid>
        <!-- Main Layout (Todoist Style) -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220" MinWidth="180" MaxWidth="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Sidebar -->
            <Border Grid.Column="0"
                    Background="{DynamicResource SidebarBrush}">
                <views:NavigationView Margin="0,16,0,0"/>
            </Border>

            <!-- Right Content Area -->
            <Border Grid.Column="1"
                    Background="{DynamicResource CardBrush}">
                <Grid Margin="32,24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header (hidden in Week view) -->
                    <StackPanel Grid.Row="0" Margin="0,0,0,16">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentView}" Value="Week">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>

                        <!-- Main Header Row -->
                        <Grid x:Name="HeaderGrid">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Left: Title -->
                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock x:Name="ViewTitleText"
                                           Text="{Binding ViewTitle}"
                                           FontSize="18"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextBrush}"
                                           VerticalAlignment="Center"/>
                                <!-- Shortcut Hints for Search and QuickAdd -->
                                <StackPanel Orientation="Horizontal"
                                            Margin="12,0,0,0"
                                            Visibility="{Binding ShowShortcuts, Converter={StaticResource BoolToVisibility}}">
                                    <Border Background="{DynamicResource AccentBrush}"
                                            CornerRadius="4"
                                            Padding="6,2"
                                            Margin="0,0,8,0">
                                        <TextBlock Text="F 검색" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                    </Border>
                                    <Border Background="{DynamicResource AccentBrush}"
                                            CornerRadius="4"
                                            Padding="6,2"
                                            Margin="0,0,8,0">
                                        <TextBlock Text="N 추가" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                    </Border>
                                    <Border Background="{DynamicResource AccentBrush}"
                                            CornerRadius="4"
                                            Padding="6,2"
                                            Margin="0,0,8,0">
                                        <TextBlock Text="D 테마" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                    </Border>
                                    <Border Background="{DynamicResource AccentBrush}"
                                            CornerRadius="4"
                                            Padding="6,2">
                                        <TextBlock Text="Z 실행취소" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                    </Border>
                                </StackPanel>
                            </StackPanel>

                            <!-- Center: Search Bar (when search mode) -->
                            <Border Grid.Column="1"
                                    Background="{DynamicResource HoverBrush}"
                                    CornerRadius="8"
                                    Padding="8,6"
                                    Margin="12,0,12,0"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Center">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSearchMode}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox x:Name="SearchTextBox"
                                             Grid.Column="0"
                                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                             FontSize="14"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Foreground="{DynamicResource TextBrush}"
                                             CaretBrush="{DynamicResource TextBrush}"
                                             VerticalAlignment="Center"
                                             TextWrapping="Wrap"
                                             AcceptsReturn="False"
                                             MaxHeight="80"
                                             KeyDown="SearchTextBox_KeyDown"/>
                                    <TextBlock Grid.Column="0"
                                               Text="검색어를 입력하세요..."
                                               FontSize="14"
                                               Foreground="{DynamicResource TextLightBrush}"
                                               VerticalAlignment="Center"
                                               IsHitTestVisible="False"
                                               Visibility="{Binding SearchText.Length, Converter={StaticResource IntToVisibilityConverter}}"/>
                                    <Button Grid.Column="1"
                                            Command="{Binding CloseSearchCommand}"
                                            Content="✕"
                                            FontSize="14"
                                            Padding="6,2"
                                            Margin="4,0,0,0"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Foreground="{DynamicResource TextLightBrush}"
                                            Cursor="Hand"
                                            ToolTip="닫기 (Esc)"/>
                                </Grid>
                            </Border>

                            <!-- Center: Quick Add Input (when QuickAdd visible) -->
                            <Border x:Name="QuickAddBorder"
                                    Grid.Column="1"
                                    Background="{DynamicResource HoverBrush}"
                                    CornerRadius="8"
                                    Padding="8,6"
                                    Margin="12,0,12,0"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Center">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsQuickAddVisible}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox x:Name="InlineQuickAddTextBox"
                                             Grid.Column="0"
                                             Text="{Binding QuickAddText, UpdateSourceTrigger=PropertyChanged}"
                                             FontSize="14"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Foreground="{DynamicResource TextBrush}"
                                             CaretBrush="{DynamicResource TextBrush}"
                                             VerticalAlignment="Center"
                                             TextWrapping="Wrap"
                                             AcceptsReturn="False"
                                             MaxHeight="80"
                                             KeyDown="InlineQuickAddTextBox_KeyDown"
                                             PreviewKeyDown="InlineQuickAddTextBox_PreviewKeyDown"/>
                                    <TextBlock Grid.Column="0"
                                               Text="할일을 입력하세요... (#태그명으로 해시태그 지정)"
                                               FontSize="14"
                                               Foreground="{DynamicResource TextLightBrush}"
                                               VerticalAlignment="Center"
                                               IsHitTestVisible="False"
                                               Visibility="{Binding QuickAddText.Length, Converter={StaticResource IntToVisibilityConverter}}"/>
                                    <Button Grid.Column="1"
                                            Command="{Binding ToggleQuickAddCommand}"
                                            Content="✕"
                                            FontSize="14"
                                            Padding="6,2"
                                            Margin="4,0,0,0"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Foreground="{DynamicResource TextLightBrush}"
                                            Cursor="Hand"
                                            ToolTip="닫기 (Esc)"/>

                                    <!-- HashTag Suggestions Popup -->
                                    <Popup x:Name="HashTagSuggestionsPopup"
                                           PlacementTarget="{Binding ElementName=InlineQuickAddTextBox}"
                                           Placement="Bottom"
                                           IsOpen="{Binding ShowHashTagSuggestions, Mode=OneWay}"
                                           StaysOpen="True"
                                           AllowsTransparency="True"
                                           PopupAnimation="Fade">
                                        <Border Background="{DynamicResource CardBrush}"
                                                BorderBrush="{DynamicResource BorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="6"
                                                Padding="4"
                                                MinWidth="200"
                                                MaxWidth="300">
                                            <Border.Effect>
                                                <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.2"/>
                                            </Border.Effect>
                                            <ListBox x:Name="HashTagSuggestionsList"
                                                     ItemsSource="{Binding HashTagSuggestions}"
                                                     SelectedIndex="{Binding SelectedSuggestionIndex, Mode=TwoWay}"
                                                     Background="Transparent"
                                                     BorderThickness="0"
                                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                                     PreviewMouseLeftButtonUp="HashTagSuggestionsList_PreviewMouseLeftButtonUp">
                                                <ListBox.ItemContainerStyle>
                                                    <Style TargetType="ListBoxItem">
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="ListBoxItem">
                                                                    <Border x:Name="border"
                                                                            Background="Transparent"
                                                                            CornerRadius="4"
                                                                            Padding="8,6"
                                                                            Cursor="Hand">
                                                                        <ContentPresenter/>
                                                                    </Border>
                                                                    <ControlTemplate.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter TargetName="border" Property="Background" Value="{DynamicResource HoverBrush}"/>
                                                                        </Trigger>
                                                                        <Trigger Property="IsSelected" Value="True">
                                                                            <Setter TargetName="border" Property="Background" Value="{DynamicResource SelectedLightBrush}"/>
                                                                        </Trigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Setter Property="Focusable" Value="False"/>
                                                    </Style>
                                                </ListBox.ItemContainerStyle>
                                                <ListBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Ellipse Width="10" Height="10"
                                                                     Fill="{Binding Color, Converter={StaticResource StringToColorBrush}}"
                                                                     Margin="0,0,6,0"
                                                                     VerticalAlignment="Center"/>
                                                            <TextBlock Text="#"
                                                                       FontWeight="Bold"
                                                                       Foreground="{Binding Color, Converter={StaticResource StringToColorBrush}}"
                                                                       Margin="0,0,2,0"/>
                                                            <TextBlock Text="{Binding Name}"
                                                                       Foreground="{DynamicResource TextBrush}"
                                                                       FontSize="13"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </ListBox.ItemTemplate>
                                            </ListBox>
                                        </Border>
                                    </Popup>
                                </Grid>
                            </Border>

                            <!-- Right: Week Days with Eggplant -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                <!-- H 완료보기 힌트 -->
                                <Border Background="{DynamicResource AccentBrush}"
                                        CornerRadius="4"
                                        Padding="6,2"
                                        Margin="0,0,8,0"
                                        VerticalAlignment="Center"
                                        Visibility="{Binding ShowShortcuts, Converter={StaticResource BoolToVisibility}}">
                                    <TextBlock Text="H 완료보기" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                </Border>
                                <Border Background="{DynamicResource HoverBrush}"
                                        CornerRadius="16"
                                        Padding="4,4"
                                        VerticalAlignment="Center">
                                <ItemsControl ItemsSource="{Binding WeekDays}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Width="32" Height="28">
                                                <!-- Day number (hidden if today) -->
                                                <TextBlock Text="{Binding Day}"
                                                           FontSize="14"
                                                           FontWeight="Medium"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"
                                                           Foreground="{DynamicResource TextLightBrush}">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsToday}" Value="True">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                                <!-- Eggplant at today position -->
                                                <Button Command="{Binding DataContext.ToggleShowCompletedCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                        ToolTip="{Binding DataContext.ShowCompletedText, RelativeSource={RelativeSource AncestorType=Window}}"
                                                        Background="Transparent"
                                                        BorderThickness="0"
                                                        Cursor="Hand"
                                                        Padding="0"
                                                        VerticalAlignment="Center"
                                                        HorizontalAlignment="Center">
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsToday}" Value="True">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Button.Style>
                                                    <Button.Template>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border x:Name="border" Background="{TemplateBinding Background}"
                                                                    CornerRadius="14"
                                                                    Width="28" Height="28">
                                                                <Canvas Width="24" Height="24" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                                    <!-- 꼭지 (초록) -->
                                                                    <Ellipse Canvas.Left="7" Canvas.Top="0" Width="10" Height="8"
                                                                             Fill="{DynamicResource CompletedBrush}"/>
                                                                    <!-- 몸통 (보라) -->
                                                                    <Ellipse Canvas.Left="3" Canvas.Top="5" Width="18" Height="18"
                                                                             Fill="{DynamicResource AccentBrush}"/>
                                                                    <!-- 눈 뜬 상태 -->
                                                                    <Ellipse x:Name="eyeLeft" Canvas.Left="7" Canvas.Top="11" Width="3" Height="3" Fill="White"/>
                                                                    <Ellipse x:Name="eyeRight" Canvas.Left="14" Canvas.Top="11" Width="3" Height="3" Fill="White"/>
                                                                    <!-- 눈 감은 상태 -->
                                                                    <Line x:Name="eyeClosedLeft" X1="6" Y1="12.5" X2="11" Y2="12.5" Stroke="White" StrokeThickness="2" Visibility="Collapsed"/>
                                                                    <Line x:Name="eyeClosedRight" X1="13" Y1="12.5" X2="18" Y2="12.5" Stroke="White" StrokeThickness="2" Visibility="Collapsed"/>
                                                                </Canvas>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource SelectedLightBrush}"/>
                                                                </Trigger>
                                                                <DataTrigger Binding="{Binding DataContext.ShowCompleted, RelativeSource={RelativeSource AncestorType=Window}}" Value="False">
                                                                    <Setter TargetName="eyeLeft" Property="Visibility" Value="Collapsed"/>
                                                                    <Setter TargetName="eyeRight" Property="Visibility" Value="Collapsed"/>
                                                                    <Setter TargetName="eyeClosedLeft" Property="Visibility" Value="Visible"/>
                                                                    <Setter TargetName="eyeClosedRight" Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Button.Template>
                                                </Button>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                </Border>
                            </StackPanel>
                        </Grid>

                        <Separator Background="{DynamicResource BorderBrush}" Margin="0,12,0,0"/>
                    </StackPanel>

                    <!-- Content Area -->
                    <Grid Grid.Row="1">
                        <!-- Task List (non-Week views) -->
                        <views:TaskListView x:Name="TaskListView">
                            <views:TaskListView.Style>
                                <Style TargetType="UserControl">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentView}" Value="Week">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </views:TaskListView.Style>
                        </views:TaskListView>

                        <!-- Week View -->
                        <views:WeekView x:Name="WeekView">
                            <views:WeekView.Style>
                                <Style TargetType="UserControl">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding DataContext.CurrentView, RelativeSource={RelativeSource AncestorType=Window}}" Value="Week">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </views:WeekView.Style>
                        </views:WeekView>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <!-- Toast Notification (하단 중앙) -->
        <Border VerticalAlignment="Bottom"
                HorizontalAlignment="Center"
                Margin="0,0,0,24"
                Visibility="{Binding ShowToast, Converter={StaticResource BoolToVisibility}}">
            <Border Background="{DynamicResource ToastBackgroundBrush}"
                    CornerRadius="8"
                    Padding="16,10">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                </Border.Effect>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding ToastMessage}"
                               Foreground="White"
                               FontSize="13"
                               VerticalAlignment="Center"/>
                    <Button Content="되돌리기"
                            Command="{Binding RedoCommand}"
                            Margin="16,0,0,0"
                            Padding="8,4"
                            FontSize="12"
                            Background="Transparent"
                            BorderThickness="1"
                            BorderBrush="{DynamicResource ToastBorderBrush}"
                            Foreground="{DynamicResource AccentBrush}"
                            Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource ToastHoverBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Border>
        </Border>

        <!-- Help Modal Overlay -->
        <Grid Visibility="{Binding ShowHelpModal, Converter={StaticResource BoolToVisibility}}">
            <!-- Dark Backdrop -->
            <Border Background="{DynamicResource OverlayBrush}"
                    MouseLeftButtonDown="HelpModalBackdrop_Click"/>

            <!-- Modal Content -->
            <Border Background="{DynamicResource CardBrush}"
                    CornerRadius="12"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    MinWidth="400"
                    MaxWidth="500"
                    Padding="24">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.3"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Grid Grid.Row="0" Margin="0,0,0,16">
                        <TextBlock Text="앱 사용법"
                                   FontSize="20"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextBrush}"/>
                        <Button Content="✕"
                                Command="{Binding CloseHelpCommand}"
                                HorizontalAlignment="Right"
                                FontSize="16"
                                Padding="4"
                                Background="Transparent"
                                BorderThickness="0"
                                Foreground="{DynamicResource TextLightBrush}"
                                Cursor="Hand"/>
                    </Grid>

                    <!-- Shortcuts List -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="400">
                        <StackPanel>
                            <!-- Navigation -->
                            <TextBlock Text="네비게이션"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextBrush}"
                                       Margin="0,0,0,8"/>
                            <Grid Margin="0,0,0,4">
                                <TextBlock Text="Ctrl + 1" FontSize="12" Foreground="{DynamicResource TextLightBrush}"/>
                                <TextBlock Text="할일" FontSize="12" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,4">
                                <TextBlock Text="Ctrl + 2" FontSize="12" Foreground="{DynamicResource TextLightBrush}"/>
                                <TextBlock Text="오늘" FontSize="12" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,4">
                                <TextBlock Text="Ctrl + 3" FontSize="12" Foreground="{DynamicResource TextLightBrush}"/>
                                <TextBlock Text="주간달력" FontSize="12" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,4">
                                <TextBlock Text="Ctrl + 4" FontSize="12" Foreground="{DynamicResource TextLightBrush}"/>
                                <TextBlock Text="다음" FontSize="12" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Right"/>
                            </Grid>

                            <Separator Margin="0,12,0,12" Background="{DynamicResource BorderBrush}"/>

                            <!-- Quick Actions -->
                            <TextBlock Text="빠른 실행"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextBrush}"
                                       Margin="0,0,0,8"/>
                            <Grid Margin="0,0,0,4">
                                <TextBlock Text="Q" FontSize="12" Foreground="{DynamicResource TextLightBrush}"/>
                                <TextBlock Text="새 할일 입력 포커스" FontSize="12" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,4">
                                <TextBlock Text="Ctrl + N" FontSize="12" Foreground="{DynamicResource TextLightBrush}"/>
                                <TextBlock Text="새 할일 입력 포커스" FontSize="12" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,4">
                                <TextBlock Text="Ctrl + F" FontSize="12" Foreground="{DynamicResource TextLightBrush}"/>
                                <TextBlock Text="검색" FontSize="12" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,4">
                                <TextBlock Text="Ctrl + Z" FontSize="12" Foreground="{DynamicResource TextLightBrush}"/>
                                <TextBlock Text="실행 취소 (Undo)" FontSize="12" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,4">
                                <TextBlock Text="Ctrl + D" FontSize="12" Foreground="{DynamicResource TextLightBrush}"/>
                                <TextBlock Text="다크 모드 전환" FontSize="12" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,4">
                                <TextBlock Text="H" FontSize="12" Foreground="{DynamicResource TextLightBrush}"/>
                                <TextBlock Text="완료 항목 보기/숨기기" FontSize="12" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,4">
                                <TextBlock Text="Esc" FontSize="12" Foreground="{DynamicResource TextLightBrush}"/>
                                <TextBlock Text="검색/모달 닫기" FontSize="12" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Right"/>
                            </Grid>

                            <Separator Margin="0,12,0,12" Background="{DynamicResource BorderBrush}"/>

                            <!-- Task Actions -->
                            <TextBlock Text="할일 관리"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextBrush}"
                                       Margin="0,0,0,8"/>
                            <Grid Margin="0,0,0,4">
                                <TextBlock Text="J / K" FontSize="12" Foreground="{DynamicResource TextLightBrush}"/>
                                <TextBlock Text="할일 목록 위/아래 이동" FontSize="12" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,4">
                                <TextBlock Text="Space" FontSize="12" Foreground="{DynamicResource TextLightBrush}"/>
                                <TextBlock Text="선택한 할일 완료 토글" FontSize="12" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,4">
                                <TextBlock Text="S" FontSize="12" Foreground="{DynamicResource TextLightBrush}"/>
                                <TextBlock Text="선택한 할일 중요 토글" FontSize="12" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,4">
                                <TextBlock Text="E / F2" FontSize="12" Foreground="{DynamicResource TextLightBrush}"/>
                                <TextBlock Text="선택한 할일 이름 수정" FontSize="12" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,4">
                                <TextBlock Text="Delete" FontSize="12" Foreground="{DynamicResource TextLightBrush}"/>
                                <TextBlock Text="선택한 할일 삭제" FontSize="12" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Right"/>
                            </Grid>

                            <Separator Margin="0,12,0,12" Background="{DynamicResource BorderBrush}"/>

                            <!-- Tips -->
                            <TextBlock Text="팁"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextBrush}"
                                       Margin="0,0,0,8"/>
                            <TextBlock TextWrapping="Wrap"
                                       FontSize="12"
                                       Foreground="{DynamicResource TextLightBrush}"
                                       LineHeight="20">
                                <Run Text="• 할일 입력 시"/>
                                <Run Text=" !내일" Foreground="{DynamicResource AccentBrush}"/>
                                <Run Text=" 또는"/>
                                <Run Text=" !다음주" Foreground="{DynamicResource AccentBrush}"/>
                                <Run Text=" 로 날짜 지정"/>
                                <LineBreak/>
                                <Run Text="• 드래그하여 할일 순서 변경"/>
                                <LineBreak/>
                                <Run Text="• 주간달력에서 드래그하여 날짜 변경"/>
                                <LineBreak/>
                                <Run Text="• 할일을 프로젝트로 드래그하여 이동"/>
                                <LineBreak/>
                                <Run Text="• 더블클릭으로 할일 이름 수정"/>
                            </TextBlock>

                            <Separator Margin="0,12,0,12" Background="{DynamicResource BorderBrush}"/>

                            <!-- Backup/Recovery -->
                            <TextBlock Text="백업/복구"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextBrush}"
                                       Margin="0,0,0,8"/>
                            <TextBlock TextWrapping="Wrap"
                                       FontSize="12"
                                       Foreground="{DynamicResource TextLightBrush}"
                                       LineHeight="20">
                                <Run Text="• 데이터는 자동으로 매일 백업됩니다 (7일 보관)"/>
                                <LineBreak/>
                                <Run Text="• 비정상 종료 시 다음 실행 때 복구 안내가 표시됩니다"/>
                                <LineBreak/>
                                <Run Text="• 아래 버튼으로 데이터를 내보내거나 가져올 수 있습니다"/>
                            </TextBlock>
                            <Grid Margin="0,8,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Button Grid.Column="0"
                                        Command="{Binding ExportCommand}"
                                        Background="{DynamicResource HoverBrush}"
                                        BorderThickness="0"
                                        Padding="8,6"
                                        Margin="0,0,4,0"
                                        Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="4"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource SelectedBrush}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="📤" FontSize="12" Margin="0,0,4,0"/>
                                        <TextBlock Text="내보내기" FontSize="12" Foreground="{DynamicResource TextBrush}"/>
                                    </StackPanel>
                                </Button>
                                <Button Grid.Column="1"
                                        Command="{Binding ImportCommand}"
                                        Background="{DynamicResource HoverBrush}"
                                        BorderThickness="0"
                                        Padding="8,6"
                                        Margin="4,0,0,0"
                                        Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="4"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource SelectedBrush}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="📥" FontSize="12" Margin="0,0,4,0"/>
                                        <TextBlock Text="가져오기" FontSize="12" Foreground="{DynamicResource TextBrush}"/>
                                    </StackPanel>
                                </Button>
                            </Grid>

                            <Separator Margin="0,12,0,12" Background="{DynamicResource BorderBrush}"/>

                            <!-- Data Reset -->
                            <TextBlock Text="데이터 초기화"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource OverdueBrush}"
                                       Margin="0,0,0,8"/>
                            <TextBlock TextWrapping="Wrap"
                                       Foreground="{DynamicResource TextLightBrush}"
                                       FontSize="12"
                                       Margin="0,0,0,8"
                                       Text="모든 할일, 프로젝트, 캐릭터 데이터가 삭제됩니다. 이 작업은 되돌릴 수 없습니다."/>
                            <Button Command="{Binding ResetDataCommand}"
                                    Background="#40E57373"
                                    BorderThickness="1"
                                    BorderBrush="{DynamicResource OverdueBrush}"
                                    Padding="8,6"
                                    Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="4"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#60E57373"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🗑️" FontSize="12" Margin="0,0,4,0"/>
                                    <TextBlock Text="데이터 초기화" FontSize="12" Foreground="{DynamicResource OverdueBrush}"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- Delete HashTag Confirmation Popup -->
        <Border Visibility="{Binding ShowDeleteHashTagConfirm, Converter={StaticResource BoolToVisibilityConverter}}"
                Background="#80000000"
                Panel.ZIndex="100">
            <Border Background="{DynamicResource CardBrush}"
                    CornerRadius="12"
                    Padding="24"
                    MaxWidth="320"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="해시태그 삭제"
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="{DynamicResource TextBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,12"/>
                    <TextBlock Foreground="{DynamicResource TextBrush}"
                               FontSize="13"
                               TextWrapping="Wrap"
                               TextAlignment="Center"
                               Margin="0,0,0,20">
                        <Run Text="'#"/><Run Text="{Binding PendingDeleteHashTagName, Mode=OneWay}"/><Run Text="' 해시태그를 삭제하시겠습니까?"/>
                        <LineBreak/>
                        <Run Text="모든 할일에서 이 태그가 제거됩니다." Foreground="{DynamicResource TextLightBrush}" FontSize="11"/>
                    </TextBlock>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Command="{Binding CancelDeleteHashTagCommand}"
                                Content="취소"
                                FontSize="13"
                                Padding="20,8"
                                Margin="0,0,12,0"
                                Background="{DynamicResource HoverBrush}"
                                Foreground="{DynamicResource TextBrush}"
                                BorderThickness="0"
                                Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="6"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource BorderBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button Command="{Binding ConfirmDeleteHashTagCommand}"
                                Content="삭제"
                                FontSize="13"
                                Padding="20,8"
                                Background="{DynamicResource OverdueBrush}"
                                Foreground="White"
                                BorderThickness="0"
                                Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="6"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#D63031"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</Window>
