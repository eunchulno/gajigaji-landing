<UserControl x:Class="SlimeTodo.Views.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:SlimeTodo.Views"
             xmlns:converters="clr-namespace:SlimeTodo.Converters">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:BoolToVisibilityInverseConverter x:Key="BoolToVisibilityInverseConverter"/>
        <converters:StringToColorBrushConverter x:Key="StringToColorBrush"/>

        <!-- Thin Overlay ScrollBar Style (Todoist/Notion style) -->
        <Style x:Key="ThinScrollBarThumb" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="thumbBorder"
                                Background="{DynamicResource TextLightBrush}"
                                CornerRadius="1.5"
                                Opacity="0.4"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="thumbBorder" Property="Opacity" Value="0.6"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="thumbBorder" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ThinOverlayScrollBar" TargetType="ScrollBar">
            <Setter Property="Width" Value="6"/>
            <Setter Property="MinWidth" Value="6"/>
            <Setter Property="Opacity" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid x:Name="scrollBarGrid" Margin="2,2,0,2">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ThinScrollBarThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ThinOverlayScrollViewer" TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <ScrollContentPresenter Margin="0,0,0,0"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                       HorizontalAlignment="Right"
                                       Orientation="Vertical"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                       Style="{StaticResource ThinOverlayScrollBar}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="PART_VerticalScrollBar"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="1" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="PART_VerticalScrollBar"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="0" Duration="0:0:0.3"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Nav Button Style with DynamicResource for theme switching -->
        <Style x:Key="LocalNavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Tag" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="indicator"
                                    Width="3"
                                    HorizontalAlignment="Left"
                                    CornerRadius="0,2,2,0"
                                    Background="Transparent"/>
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="5"
                                    Padding="{TemplateBinding Padding}"
                                    Margin="4,0,0,0">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <!-- Non-selected hover -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="Tag" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource HoverBrush}"/>
                            </MultiTrigger>
                            <!-- Selected state - show indicator bar -->
                            <Trigger Property="Tag" Value="True">
                                <Setter TargetName="indicator" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource SelectedLightBrush}"/>
                            </Trigger>
                            <!-- Selected hover -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="Tag" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource HoverBrush}"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LocalNavButtonSelectedStyle" TargetType="Button" BasedOn="{StaticResource LocalNavButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource SelectedLightBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="FontWeight" Value="Medium"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>  <!-- Projects -->
            <RowDefinition Height="*"/>     <!-- HashTags -->
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Main Navigation -->
        <StackPanel Grid.Row="0" Margin="8,8,8,4">
            <!-- App Title Header -->
            <TextBlock Text="가지가지"
                       FontSize="11"
                       FontWeight="Bold"
                       Foreground="{DynamicResource TextLightBrush}"
                       Margin="4,0,0,8"/>

            <Grid Margin="0,2">
                <Button x:Name="InboxButton"
                        Command="{Binding NavigateCommand}"
                        CommandParameter="Inbox"
                        AllowDrop="True"
                        Drop="NavButton_Drop"
                        DragOver="NavButton_DragOver">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource LocalNavButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsInboxSelected}" Value="True">
                                    <Setter Property="Tag" Value="True"/>
                                    <Setter Property="FontWeight" Value="Medium"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📥" FontSize="14" Margin="0,0,10,0" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsInboxSelected}" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock Text="할일" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsInboxSelected}" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Button>
                <!-- Shortcut Badge -->
                <Border HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Margin="0,0,12,0"
                        Background="{DynamicResource AccentBrush}"
                        CornerRadius="4"
                        Padding="6,2"
                        Visibility="{Binding ShowShortcuts, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="1" FontSize="11" FontWeight="Bold" Foreground="White"/>
                </Border>
            </Grid>

            <Grid Margin="0,1">
                <Button Command="{Binding NavigateCommand}"
                        CommandParameter="Today"
                        AllowDrop="True"
                        Drop="NavButton_Drop"
                        DragOver="NavButton_DragOver">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource LocalNavButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsTodaySelected}" Value="True">
                                    <Setter Property="Tag" Value="True"/>
                                    <Setter Property="FontWeight" Value="Medium"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="☀️" FontSize="14" Margin="0,0,10,0" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsTodaySelected}" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock Text="오늘" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsTodaySelected}" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Button>
                <!-- Shortcut Badge -->
                <Border HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Margin="0,0,12,0"
                        Background="{DynamicResource AccentBrush}"
                        CornerRadius="4"
                        Padding="6,2"
                        Visibility="{Binding ShowShortcuts, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="2" FontSize="11" FontWeight="Bold" Foreground="White"/>
                </Border>
            </Grid>

            <Grid Margin="0,1">
                <Button Command="{Binding NavigateCommand}"
                        CommandParameter="Week"
                        AllowDrop="True"
                        Drop="NavButton_Drop"
                        DragOver="NavButton_DragOver">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource LocalNavButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsWeekSelected}" Value="True">
                                    <Setter Property="Tag" Value="True"/>
                                    <Setter Property="FontWeight" Value="Medium"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🗓️" FontSize="14" Margin="0,0,10,0" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsWeekSelected}" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock Text="주간달력" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsWeekSelected}" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Button>
                <!-- Shortcut Badge -->
                <Border HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Margin="0,0,12,0"
                        Background="{DynamicResource AccentBrush}"
                        CornerRadius="4"
                        Padding="6,2"
                        Visibility="{Binding ShowShortcuts, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="3" FontSize="11" FontWeight="Bold" Foreground="White"/>
                </Border>
            </Grid>

            <Grid Margin="0,1">
                <Button Command="{Binding NavigateCommand}"
                        CommandParameter="Upcoming"
                        AllowDrop="True"
                        Drop="NavButton_Drop"
                        DragOver="NavButton_DragOver">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource LocalNavButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsUpcomingSelected}" Value="True">
                                    <Setter Property="Tag" Value="True"/>
                                    <Setter Property="FontWeight" Value="Medium"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📅" FontSize="14" Margin="0,0,10,0" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsUpcomingSelected}" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock Text="다음" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsUpcomingSelected}" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Button>
                <!-- Shortcut Badge -->
                <Border HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Margin="0,0,12,0"
                        Background="{DynamicResource AccentBrush}"
                        CornerRadius="4"
                        Padding="6,2"
                        Visibility="{Binding ShowShortcuts, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="4" FontSize="11" FontWeight="Bold" Foreground="White"/>
                </Border>
            </Grid>
        </StackPanel>

        <!-- Projects Section -->
        <StackPanel Grid.Row="1" Margin="8,8,8,4">
            <!-- Projects Header -->
            <Grid Margin="4,8,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0"
                        Command="{Binding ToggleProjectsExpandedCommand}"
                        Background="Transparent"
                        BorderThickness="0"
                        Padding="0"
                        Margin="0,0,4,0"
                        Cursor="Hand"
                        VerticalAlignment="Center">
                    <TextBlock FontSize="8" Foreground="{DynamicResource TextLightBrush}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="▶"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsProjectsExpanded}" Value="True">
                                        <Setter Property="Text" Value="▼"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Button>
                <TextBlock Grid.Column="1"
                           Text="프로젝트"
                           FontSize="11"
                           FontWeight="Bold"
                           Foreground="{DynamicResource TextLightBrush}"
                           VerticalAlignment="Center"/>
                <Button Grid.Column="2"
                        Command="{Binding StartAddProjectCommand}"
                        Content="+"
                        FontSize="14"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="{DynamicResource TextLightBrush}"
                        Padding="8,2"
                        Cursor="Hand"/>
            </Grid>

            <!-- Add Project Input -->
            <Grid Margin="4,0,4,4" Visibility="{Binding IsAddingProject, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Text="{Binding NewProjectName, UpdateSourceTrigger=PropertyChanged}"
                             FontSize="12"
                             Padding="6,4"
                             Background="{DynamicResource CardBrush}"
                             Foreground="{DynamicResource TextBrush}"
                             BorderBrush="{DynamicResource AccentBrush}"
                             BorderThickness="1"
                             CaretBrush="{DynamicResource TextBrush}"
                             KeyDown="AddProjectTextBox_KeyDown"
                             Loaded="AddProjectTextBox_Loaded"/>
                    <Button Grid.Column="1"
                            Command="{Binding CancelAddProjectCommand}"
                            Content="✕"
                            FontSize="10"
                            Background="Transparent"
                            BorderThickness="0"
                            Foreground="{DynamicResource TextLightBrush}"
                            Padding="4,0"
                            Cursor="Hand"/>
                </Grid>

            <!-- Projects List -->
            <ItemsControl ItemsSource="{Binding Projects}"
                          Visibility="{Binding IsProjectsExpanded, Converter={StaticResource BoolToVisibilityConverter}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding SelectCommand}"
                                    Margin="0,2"
                                    Cursor="Hand"
                                    AllowDrop="True"
                                    Drop="ProjectButton_Drop"
                                    DragOver="ProjectButton_DragOver"
                                    PreviewMouseLeftButtonDown="ProjectButton_PreviewMouseLeftButtonDown"
                                    PreviewMouseMove="ProjectButton_PreviewMouseMove">
                                <Button.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="이름 변경" Command="{Binding StartEditCommand}"/>
                                        <Separator/>
                                        <MenuItem Header="삭제" Command="{Binding DeleteCommand}" Foreground="#E85D75"/>
                                    </ContextMenu>
                                </Button.ContextMenu>
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Padding" Value="10,6"/>
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="border"
                                                            Background="{TemplateBinding Background}"
                                                            CornerRadius="6"
                                                            Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="border" Property="Background" Value="{DynamicResource HoverBrush}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource SelectedLightBrush}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Selection Indicator (left bar) -->
                                    <Border Width="3"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Stretch"
                                            CornerRadius="0,2,2,0"
                                            Margin="-10,0,0,0"
                                            Grid.ColumnSpan="3">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                    </Border>

                                    <!-- Folder Icon -->
                                    <TextBlock Text="📁"
                                               FontSize="10"
                                               VerticalAlignment="Center"
                                               Margin="0,0,8,0"/>

                                    <!-- Normal View -->
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding Name}"
                                               FontSize="12"
                                               VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis"
                                               Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibilityInverseConverter}}">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                        <Setter Property="FontWeight" Value="Medium"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>

                                    <!-- Edit View -->
                                    <TextBox Grid.Column="1"
                                             Text="{Binding EditName, UpdateSourceTrigger=PropertyChanged}"
                                             FontSize="12"
                                             Padding="4,2"
                                             Background="{DynamicResource CardBrush}"
                                             Foreground="{DynamicResource TextBrush}"
                                             BorderBrush="{DynamicResource AccentBrush}"
                                             BorderThickness="1"
                                             Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibilityConverter}}"
                                             KeyDown="EditProjectTextBox_KeyDown"
                                             LostFocus="EditProjectTextBox_LostFocus"/>

                                    <!-- Task Count Badge -->
                                    <Border Grid.Column="2"
                                            CornerRadius="8"
                                            Padding="6,2"
                                            MinWidth="20"
                                            VerticalAlignment="Center"
                                            Margin="8,0,0,0"
                                            Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibilityInverseConverter}}">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding TaskCount}" Value="0">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding TaskCount}"
                                                   FontSize="10"
                                                   FontWeight="Medium"
                                                   Foreground="{DynamicResource TextLightBrush}"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"/>
                                    </Border>
                                </Grid>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            <!-- Empty Projects State -->
            <TextBlock Text="+ 버튼으로 프로젝트를 추가하세요"
                       FontSize="10"
                       Foreground="{DynamicResource TextLightBrush}"
                       Margin="4,4,0,0"
                       Opacity="0.7">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding Projects.Count}" Value="0"/>
                                    <Condition Binding="{Binding IsProjectsExpanded}" Value="True"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible"/>
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </StackPanel>

        <!-- HashTags Section -->
        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto"
                      Margin="8,8,8,4"
                      Style="{StaticResource ThinOverlayScrollViewer}">
            <StackPanel>
                <!-- HashTags Header -->
                <Grid Margin="4,8,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0"
                            Command="{Binding ToggleHashTagsExpandedCommand}"
                            Background="Transparent"
                            BorderThickness="0"
                            Padding="0"
                            Margin="0,0,4,0"
                            Cursor="Hand"
                            VerticalAlignment="Center">
                        <TextBlock FontSize="8" Foreground="{DynamicResource TextLightBrush}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="▶"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsHashTagsExpanded}" Value="True">
                                            <Setter Property="Text" Value="▼"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Button>
                    <TextBlock Grid.Column="1"
                               Text="해시태그"
                               FontSize="11"
                               FontWeight="Bold"
                               Foreground="{DynamicResource TextLightBrush}"
                               VerticalAlignment="Center"/>
                </Grid>

                <!-- HashTags List -->
                <ItemsControl ItemsSource="{Binding HashTags}"
                              Visibility="{Binding IsHashTagsExpanded, Converter={StaticResource BoolToVisibilityConverter}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding SelectCommand}"
                                    Margin="0,2"
                                    Cursor="Hand">
                                <Button.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="빨강" Command="{Binding SetColorCommand}" CommandParameter="#E74C3C">
                                            <MenuItem.Icon>
                                                <Ellipse Width="12" Height="12" Fill="#E74C3C"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem Header="주황" Command="{Binding SetColorCommand}" CommandParameter="#E67E22">
                                            <MenuItem.Icon>
                                                <Ellipse Width="12" Height="12" Fill="#E67E22"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem Header="노랑" Command="{Binding SetColorCommand}" CommandParameter="#F1C40F">
                                            <MenuItem.Icon>
                                                <Ellipse Width="12" Height="12" Fill="#F1C40F"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem Header="초록" Command="{Binding SetColorCommand}" CommandParameter="#2ECC71">
                                            <MenuItem.Icon>
                                                <Ellipse Width="12" Height="12" Fill="#2ECC71"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem Header="청록" Command="{Binding SetColorCommand}" CommandParameter="#1ABC9C">
                                            <MenuItem.Icon>
                                                <Ellipse Width="12" Height="12" Fill="#1ABC9C"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem Header="파랑" Command="{Binding SetColorCommand}" CommandParameter="#3498DB">
                                            <MenuItem.Icon>
                                                <Ellipse Width="12" Height="12" Fill="#3498DB"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem Header="보라" Command="{Binding SetColorCommand}" CommandParameter="#9B59B6">
                                            <MenuItem.Icon>
                                                <Ellipse Width="12" Height="12" Fill="#9B59B6"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem Header="분홍" Command="{Binding SetColorCommand}" CommandParameter="#E91E63">
                                            <MenuItem.Icon>
                                                <Ellipse Width="12" Height="12" Fill="#E91E63"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem Header="회색" Command="{Binding SetColorCommand}" CommandParameter="#95A5A6">
                                            <MenuItem.Icon>
                                                <Ellipse Width="12" Height="12" Fill="#95A5A6"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem Header="진회색" Command="{Binding SetColorCommand}" CommandParameter="#34495E">
                                            <MenuItem.Icon>
                                                <Ellipse Width="12" Height="12" Fill="#34495E"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <Separator/>
                                        <MenuItem Header="숨기기" Command="{Binding HideCommand}">
                                            <MenuItem.Style>
                                                <Style TargetType="MenuItem">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsHidden}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </MenuItem.Style>
                                        </MenuItem>
                                        <MenuItem Header="복원" Command="{Binding RestoreCommand}">
                                            <MenuItem.Style>
                                                <Style TargetType="MenuItem">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsHidden}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </MenuItem.Style>
                                        </MenuItem>
                                        <MenuItem Header="삭제" Command="{Binding DeleteCommand}" Foreground="#E85D75"/>
                                    </ContextMenu>
                                </Button.ContextMenu>
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Padding" Value="10,6"/>
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="border"
                                                            Background="{TemplateBinding Background}"
                                                            CornerRadius="6"
                                                            Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="border" Property="Background" Value="{DynamicResource HoverBrush}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource SelectedLightBrush}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <Grid>
                                    <Grid.Style>
                                        <Style TargetType="Grid">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsHidden}" Value="True">
                                                    <Setter Property="Opacity" Value="0.5"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Grid.Style>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Selection Indicator (left bar) -->
                                    <Border Width="3"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Stretch"
                                            CornerRadius="0,2,2,0"
                                            Margin="-10,0,0,0"
                                            Grid.ColumnSpan="3">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                        <Setter Property="Background" Value="{Binding Color, Converter={StaticResource StringToColorBrush}}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                    </Border>

                                    <!-- Color Dot -->
                                    <Ellipse Width="10" Height="10"
                                             Fill="{Binding Color, Converter={StaticResource StringToColorBrush}}"
                                             VerticalAlignment="Center"
                                             Margin="0,0,8,0"/>

                                    <!-- HashTag Name with # symbol in color -->
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="#"
                                                   FontSize="12"
                                                   FontWeight="Bold"
                                                   Foreground="{Binding Color, Converter={StaticResource StringToColorBrush}}"
                                                   Margin="0,0,1,0"/>
                                        <TextBlock Text="{Binding Name}"
                                                   FontSize="12"
                                                   TextTrimming="CharacterEllipsis">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                            <Setter Property="FontWeight" Value="Medium"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock Text="(숨김)"
                                                   FontSize="10"
                                                   Foreground="{DynamicResource TextLightBrush}"
                                                   Margin="4,0,0,0"
                                                   VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsHidden}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>

                                    <!-- Task Count Badge -->
                                    <Border Grid.Column="2"
                                            CornerRadius="8"
                                            Padding="6,2"
                                            MinWidth="20"
                                            VerticalAlignment="Center"
                                            Margin="8,0,0,0">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding TaskCount}" Value="0">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding TaskCount}"
                                                   FontSize="10"
                                                   FontWeight="Medium"
                                                   Foreground="{DynamicResource TextLightBrush}"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"/>
                                    </Border>
                                </Grid>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Empty State -->
                <TextBlock Text="할일에 #태그 를 추가해보세요"
                           FontSize="10"
                           Foreground="{DynamicResource TextLightBrush}"
                           Margin="4,8,0,0"
                           Opacity="0.7">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding HashTags.Count}" Value="0"/>
                                        <Condition Binding="{Binding IsHashTagsExpanded}" Value="True"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Visible"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- Show Hidden HashTags Button -->
                <Button Command="{Binding ToggleShowHiddenHashTagsCommand}"
                        Background="Transparent"
                        BorderThickness="0"
                        Cursor="Hand"
                        Margin="4,8,0,0"
                        HorizontalAlignment="Left">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Opacity" Value="0.7"/>
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasHiddenHashTags}" Value="False">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsHashTagsExpanded}" Value="False">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="1"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontSize="10" Foreground="{DynamicResource AccentBrush}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="숨긴 태그 보기"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ShowHiddenHashTags}" Value="True">
                                            <Setter Property="Text" Value="숨긴 태그 가리기"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Button>
            </StackPanel>
        </ScrollViewer>

        <!-- Pet -->
        <views:PetView Grid.Row="3"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Bottom"
                       Margin="0,8,0,16"/>

        <!-- Help Button -->
        <Button Grid.Row="4"
                x:Name="HelpButton"
                Command="{Binding ShowHelpCommand}"
                Background="Transparent"
                BorderThickness="0"
                HorizontalContentAlignment="Center"
                Padding="8,6"
                Margin="8,0,8,16"
                MinHeight="32"
                Cursor="Hand">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="❓" FontSize="12" Margin="0,0,6,0" Opacity="0.7"/>
                <TextBlock Text="앱 사용법"
                           FontSize="12"
                           Foreground="{DynamicResource TextLightBrush}"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Button>
    </Grid>
</UserControl>
